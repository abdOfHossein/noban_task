{
  "swagger": "2.0",
  "info": {
    "version": "1.0.0",
    "title": "appointmetSystem_Task of Noban",
    "description": "A service for give appointmet to client with selected doctor",
    "license": {
      "name": "MIT",
      "url": "https://opensource.org/licenses/MIT"
    }
  },
  "host": "localhost:3000",
  "basePath": "/",
  "tags": [
    {
      "name": "Ticket",
      "description": "API for Tickets in the system"
    }
  ],
  "schemes": ["http"],
  "consumes": ["application/json"],
  "produces": ["application/json"],
  "paths": {
    "/ticket": {
      "post": {
        "tags": ["Ticket"],
        "summary": "give appointment to the client",
        "responses": {
          "201": {
            "description": "OK",
            "schema": {
              "$ref": "#/definitions/Tickets"
            }
          }
        }
      }
    }
  },
  "definitions": {
    "Ticket": {
      "required": ["name", "_id", "companies"],
      "properties": {
        "_id": {
          "type": "integer",
          "uniqueItems": true
        },
        "userId": {
          "type": "integer"
        },
        "doctorId": {
          "type": "integer"
        }
      }
    },
    "Tickets": {
      "type": "array",
      "$ref": "#/definitions/Tickets"
    }
  }
}
